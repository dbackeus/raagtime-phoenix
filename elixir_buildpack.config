erlang_version=17.5
elixir_version=1.0.4
always_rebuild=true

# Export heroku config vars
config_vars_to_export=(DATABASE_URL DATABASE_POOL HOST SECRET_KEY_BASE)

# A command to run right after compiling the app
# post_compile="pwd"
